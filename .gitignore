# Logs
logs
*.log
npm-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# TypeScript incremental build info
*.tsbuildinfo

# Claude Code local settings
.claude/settings.local.json

# MCP config enthält Secrets (Supabase Access Token) -> niemals committen
.mcp.json

# Environment / Secrets
.env
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Fragen-Exporte / Rohdaten der Kurse -> nicht ins (oeffentliche) Repo
weg-fragen-export.json
hg-fragen-gesamt.json
*-fragen-*.json
*fragen*.json

# Pruefungs-Dokumente / Bearbeitungsboegen -> nicht ins (oeffentliche) Repo
# (Marken-/Design-Vorlagen wie ifmera-Vorlage.docx bleiben erlaubt)
*Bearbeitungsbogen*.docx
*Pruefung*.docx
*Pruefung*.pdf